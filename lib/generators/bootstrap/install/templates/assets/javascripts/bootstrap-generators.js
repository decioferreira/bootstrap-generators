// Select all jQuery plugins
//= require bootstrap

// Or quickly add only the necessary javascript (removing the space before the equal sign)
// = require bootstrap-transition	// (required for any animation)
// = require bootstrap-alert
// = require bootstrap-modal
// = require bootstrap-dropdown
// = require bootstrap-scrollspy
// = require bootstrap-tab
// = require bootstrap-tooltip
// = require bootstrap-popover		// (requires Tooltips)
// = require bootstrap-button
// = require bootstrap-collapse
// = require bootstrap-carousel
// = require bootstrap-typeahead
